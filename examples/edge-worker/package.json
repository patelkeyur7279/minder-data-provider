{
  "name": "minder-edge-worker-example",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker example proving minder-data-provider's edge (workerd) path — Support Matrix evidence.",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "start": "wrangler dev --port 8787"
  },
  "dependencies": {
    "minder-data-provider": "file:../../minder-data-provider-2.2.0-beta.0.tgz"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
