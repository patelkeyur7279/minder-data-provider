/**
 * Logger exports for minder-data-provider
 *
 * This module contains the default logger instance.
 * Import this separately to avoid including it in your main bundle.
 *
 * @example
 * ```typescript
 * import { defaultLogger } from 'minder-data-provider/logger';
 *
 * defaultLogger.info('Application started');
 * ```
 */

export { defaultLogger } from '../utils/Logger.js';